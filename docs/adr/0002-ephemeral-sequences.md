# ADR-0002：原始序列不长期保存

- 状态：Accepted
- 日期：2026-07-28

## Context

科研序列可能未公开。用户已明确要求不长期保存序列。

## Decision

原始序列只在任务临时工作区存在，任务完成后尽快删除，任何情况下不超过 24 小时。
数据库只保存任务元数据、输入摘要、参考版本和结果 provenance。日志不得包含序列正文。

## Consequences

降低泄露和合规风险；代价是任务过期后不可恢复或重新下载。用户需要重新提交才能复算。
若未来提供历史记录，默认保存参数与公共 reference 标识，不保存私有序列。
